name = "wecull"
compatibility_date = "2026-06-19"

# Static site — no Worker script, just serve the files in this repo.
# Cloudflare serves privacy.html at /privacy and terms.html at /terms automatically.
[assets]
directory = "./"
